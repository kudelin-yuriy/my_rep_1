//  This is configuration file for the UTF8-valiadtor program. 
//  It contains as many sections as many tables / columns you want to check. Each section must describe the table name, ID column 
//(need to display the record id in the output file), and name of the column that potentially has invalid UTF8-codes. 

 TABLE=DSCSTG.PRTNR_SIGN_SPLIT
 ID_COLUMN=CUST_NUM
 STR_COLUMN=CUST_NM
//////////////////////////////////////////////////////////////////////////

 TABLE=DSCSTG.PRTNR_MNG_REV
 ID_COLUMN=CUST_NUM
 STR_COLUMN=CUST_NM
//////////////////////////////////////////////////////////////////////////

 TABLE=DSCSTG.PRTNR_PROFILE
 ID_COLUMN=CNUM_ID
 STR_COLUMN=LAST_NM
//////////////////////////////////////////////////////////////////////////
